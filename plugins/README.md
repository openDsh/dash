# Plugins

Each folder in this directory builds into a Qt plugin of the same name.

Nix package builds install plugins under `lib/plugins/<type>`. Local CMake builds place them under the build directory at `lib/plugins/<type>`.
